/**
 * Web decisions plugin, node half.
 *
 * The Decisions conversation view is browser-only: it reads the session
 * workspace's `DECISIONS.md` decision register over the `workspaceFiles`
 * Remote and renders the register table in the page.
 */

/** Host plugin body — every contribution lives in the browser half. */
export function apply(): void {}
