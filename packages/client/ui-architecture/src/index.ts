/**
 * Web architecture plugin, node half.
 *
 * The Architecture conversation view is browser-only: it reads the session
 * workspace's project documents over the `workspaceFiles` Remote and renders
 * the Mermaid diagram in the page.
 */

/** Host plugin body — every contribution lives in the browser half. */
export function apply(): void {}
