/**
 * Web token-usage meter plugin, node half.
 *
 * The meter is a browser surface over the host-computed usage projections;
 * nothing runs on the host side.
 */

/** Host plugin body — the meter reads projections computed elsewhere. */
export function apply(): void {}
